# Validation fixture — set_pool references a pool not declared in pools:.
schema_version: 1
listeners:
  listeners:
    - address: "127.0.0.1:15353"
      protocol: udp
pools:
  - name: primary
    backends:
      - address: "127.0.0.1:5300"
        weight: 100
rules:
  match_mode: first_match
  rules:
    - name: typo-pool
      hook: request
      selectors: []
      actions:
        - type: set_pool
          value: primry
