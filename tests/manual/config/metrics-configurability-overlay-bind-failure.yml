# G4 manual lab: bind-failure overlay — uses privileged port 80.
# Apply this overlay to test that bind failure rejects apply and keeps last-good.
schema_version: 1
metrics:
  prometheus:
    listen_address: "127.0.2.1:80"
    path: /metrics
