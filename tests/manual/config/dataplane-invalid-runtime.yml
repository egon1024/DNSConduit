# Phase 1d — invalid runtime (must fail validate).
schema_version: 1
dataplane:
  runtime: not_a_runtime
listeners:
  threads: 1
  listeners:
    - address: "127.0.2.1:15353"
      protocol: udp
pools:
  - name: default
    backends:
      - address: "127.0.2.1:15300"
