schema_version: 1
id: fixture-auth-a
suites: [full]
intent: |
  Query a harness-owned zone name for A against an authoritative peer through
  Conduit and match the committed fixture RRset (oracle: fixture).
applicability:
  roles: [auth]
  profiles: [forward-only]
peer_setup:
  fixtures: [example.test]
oracles:
  - kind: fixture
    path: fixtures/zones/example.test/expected-a.json
