# Python packages for operator documentation (MkDocs Material, mike).
# Not used by the Rust workspace — only for building and serving operator-docs/.
#
#   pip install -r requirements.txt
#   make docs-serve

mkdocs==1.6.1
mkdocs-material==9.5.49
mike==2.1.3
