schema_version: 1
id: lifecycle-config-apply
suite: lifecycle
curated: true
summary: |
  How long a conduitctl apply takes for a sparse overlay that only changes the logging level.
notes: |
  Small lifecycle cell for local runs; not required in the published reference set.
axes:
  runtime: sync
  load_shape: forward_fast
  obs_posture: metrics_off
  ingress_workers: 2
recipe:
  config: lifecycle-config-apply-base.yml
  overlay: lifecycle-config-apply-overlay.yml
  upstream: fast
  loadgen: none
  lifecycle: config_apply
