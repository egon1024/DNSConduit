schema_version: 1
id: scale-sync-ingress-8-forward-slow
suite: scale
curated: true
summary: |
  Sync runtime with eight ingress workers under a slow stub upstream.
notes: |
  Optional noisy rung in the ingress series — keep in the catalog; publish may omit it if the lab is unstable.
axes:
  runtime: sync
  load_shape: forward_slow
  ingress_workers: 8
recipe:
  config: scale-sync-ingress-8-obs-off.yml
  upstream: slow
  loadgen: dnsperf
  query_file: perf-a.txt
  clients: 16
  dnsperf_threads: 8
  max_outstanding: 2000
  duration_s: 30
